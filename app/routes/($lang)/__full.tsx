import { MetaDescriptor } from '@remix-run/react'
import { DynamicLayout } from 'app/client/components/DynamicLayout'

export const meta: Array<MetaDescriptor> = []

export default () => <DynamicLayout fill />
